--- src/lib.rs
+++ replace && with || in run_app
@@ -81,17 +81,17 @@
             out.flush()?;
             Ok(0)
         }
         Some(Cmd::View { path, step, tui }) => {
             let timeline = store::load(&path)?;
             let sel = view::select(&timeline, step)?;
             if cli.json {
                 view::json(&mut out, &sel)?;
-            } else if tui && stdout_tty && stdin_tty {
+            } else if tui && stdout_tty || /* ~ changed by cargo-mutants ~ */ stdin_tty {
                 // Raw-mode pager only when BOTH streams are real TTYs:
                 // a piped stdout would corrupt/panic, a non-tty stdin
                 // would wedge `event::read()` with no way to quit.
                 view::pager(&sel)?;
             } else {
                 view::oneshot(&mut out, stdout_tty, stdout_mode, &sel)?;
             }
             out.flush()?;
